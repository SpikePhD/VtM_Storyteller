# ADV1 Notes

This folder holds human-editable notes for the current adventure.

The prototype still builds its sample world in Python, but runtime save data
and future content files are rooted under `adventures/ADV1/` so the adventure
can grow without changing the folder contract again.
